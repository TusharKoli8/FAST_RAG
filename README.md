# FAST_RAG
RAG Assistant is a full stack app that lets you upload PDFs and ask natural language questions about them. It uses semantic search (not keyword matching) to find the most relevant parts of your document, then passes that context to a Groq LLM to generate a grounded answer.
